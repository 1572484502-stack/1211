# 视界工坊模块边界

视界工坊采用“轻量外壳 + 独立功能模块”结构，不引入前端框架。新增功能不得直接读取或修改其他功能的内部状态。

## 目录约定

```text
src/
  core/                  # 稳定公共层，修改频率应最低
    runtime.js           # 功能注册、激活与生命周期
    media-session.js     # 全局唯一播放控制
    storage.js           # 按功能隔离的本地存储
    shell.js             # 桌面窗口外壳
  features/
    music/               # 音乐功能（逐步迁入）
server.cjs               # 仅监听 127.0.0.1，桥接需要 Node/文件系统的本地能力
```

## 强制规则

1. 每个功能使用唯一 `featureId`，独立管理界面、状态、算法和样式。
2. 功能之间不得互相引用文件；只能调用 `VistaCore` 注册的公共服务或发送事件。
3. 本地存储必须使用 `VistaStore.read/write(featureId, key)`，避免键名冲突。
4. 音频和视频播放必须注册到 `VistaMedia`，确保全软件只播放一个媒体。
5. 新功能样式使用独立的功能前缀，禁止覆盖音乐模块选择器。
6. 公共层只放至少两个功能都需要的能力；单一功能逻辑不得进入 `core/`。
7. 修改一个功能时，测试范围默认只包含该功能及其公共接口契约。
8. 需要系统能力的功能通过 `server.cjs` 适配，功能模块不得直接执行系统命令。

## 功能生命周期

每个功能向 `VistaCore.registerFeature()` 提供：

- `id`：唯一标识；
- `activate()`：进入功能时执行；
- `deactivate()`：离开功能时清理播放、计时器或临时任务。

这套边界保证音乐算法的调整不会牵连后续功能代码。
